

export default function DogCollectionBreedsGallery({ imgSrcs }) {


  return (
    <>
      

      {/* <main>
        <section>
          {imgSrcs.map((img, idx) => (
            <img key={idx} alt='' src={img} />
          ))}
        </section>
      </main> */}
    </>
  )
}
